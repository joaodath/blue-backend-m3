const nome = 'Calculadora do João';

const soma = (a, b) => a + b;

const subtracao = (a, b) => a - b;

const multi = (a, b) => a * b;

const div = (a, b) => a / b;

module.exports = { 
    nome,
    soma,
    subtracao,
    multi,
    div 
};